import React, { useEffect, useRef, useState } from "react";
import { Bot, Camera, CheckCircle2, ChevronRight, Download, ExternalLink, Lightbulb, Link2, MapPin, Mic, Plus, RefreshCw, Route, ShieldCheck, Smartphone, Trash2, Upload, UserRound } from "lucide-react";
import { json, jsonOptions } from "../api.js";
import { AppLink } from "../components/Shell.jsx";
import { Empty, Loading, StatusDot, Tabs } from "../components/Ui.jsx";
import { AgentChat } from "./KnowledgeAgent.jsx";
import { MEMBERS, TEAM } from "../team.js";

export function FieldApp({ search }) {
  const [memberId, setMemberId] = useState(() => new URLSearchParams(search).get("memberId") || localStorage.getItem("rbcc-member-id") || "");
  const [tab, setTab] = useState(() => new URLSearchParams(search).get("tab") === "agent" ? "agent" : "route");
  const member = MEMBERS.find(item => item.id === memberId);
  useEffect(() => { if (memberId) localStorage.setItem("rbcc-member-id", memberId); }, [memberId]);
  if (!member) return <IdentityPicker onSelect={setMemberId} />;
  return <div className="field-app"><DesktopLinks/><header className="mobile-header"><div><small>{TEAM.shortName} · 队员端</small><strong>{member.name}</strong></div><button onClick={() => setMemberId("")}><UserRound size={16}/>切换身份</button></header><div className="field-content">{tab === "agent" ? <AgentChat memberId={memberId} memberName={member.name} mobile/> : <RouteCapture member={member}/>}</div><nav className="mobile-nav"><button className={tab === "route" ? "active" : ""} onClick={() => setTab("route")}><Route size={21}/>走访路线</button><button className={tab === "agent" ? "active" : ""} onClick={() => setTab("agent")}><Bot size={21}/>红小八</button></nav></div>;
}

function IdentityPicker({ onSelect }) {
  return <div className="identity-page"><DesktopLinks/><header><div><small>{TEAM.shortName} · 队员端</small><span>请先选择身份</span></div><button title="检查并加载最新版本"><RefreshCw size={15}/>立即更新</button></header><main><div className="phone-mark"><Smartphone size={30}/></div><h1>选择你的身份</h1><p>{TEAM.name} · {TEAM.theme}</p><small className="route-label"><i/>{TEAM.mascot}</small><div className="identity-list">{MEMBERS.map(member => <button key={member.id} onClick={() => onSelect(member.id)}><span className="avatar"><UserRound size={19}/></span><span><strong>{member.name}</strong><small>{member.role}</small></span><ChevronRight size={19}/></button>)}</div><section className="install-hint"><Download size={22}/><span><strong>安装到手机桌面</strong><p>使用浏览器菜单中的「添加到主屏幕」或「安装应用」。</p></span></section></main><nav><span><Route size={21}/>走访路线</span><span><Bot size={21}/>红小八</span></nav></div>;
}

function DesktopLinks(){return <aside className="field-desktop-links"><AppLink href="/screen"><Smartphone size={18}/><span><strong>智能呈现 · 桌面协同</strong><small>查看全组进度、受阻项与路演</small></span></AppLink><nav>{[["/collab","协同 Hub"],["/review","评审"],["/","作战室"],["/library","节点库"],["/knowledge","知识中心"],["/agent","红小八"],["/traces","留痕库"],["/dashboard","调研报告"],["/admin","管理端"]].map(([href,label])=><AppLink key={href} href={href}>{label}</AppLink>)}</nav></aside>}

function RouteCapture({ member }) {
  const [dashboard, setDashboard] = useState(null);
  const [active, setActive] = useState("");
  const [panel, setPanel] = useState("questions");
  const [questions, setQuestions] = useState([]);
  const [text, setText] = useState("");
  const [status, setStatus] = useState("");
  const [problem, setProblem] = useState({ title:"", evidence:"", severity:"normal" });
  const [solution, setSolution] = useState({ title:"", description:"", testPlan:"", metrics:"" });
  const [problems, setProblems] = useState([]);
  const [solutions, setSolutions] = useState([]);
  const [observations,setObservations]=useState([]);
  const [evidenceKind,setEvidenceKind]=useState("observation");
  const [busy, setBusy] = useState("");
  const [questionDraft, setQuestionDraft] = useState(null);
  const [reference, setReference] = useState({ matched:false, questions:[], clues:[] });
  const [problemNotes, setProblemNotes] = useState({});
  const [solutionResults, setSolutionResults] = useState({});
  const fileRef = useRef(null);

  useEffect(() => { json(`/api/research-dashboard?memberId=${member.id}`).then(value => { setDashboard(value); setActive(value.members?.[0]?.sites?.[0]?.companyId || ""); }); }, [member.id]);
  const sites = dashboard?.members?.[0]?.sites ?? [];
  const site = sites.find(item => item.companyId === active);

  async function loadSite() {
    if (!site) return;
    const [questionData, problemData, solutionData, referenceData, dashboardData] = await Promise.all([
      json(`/api/research-questions?memberId=${member.id}&companyId=${site.companyId}`),
      json(`/api/problems?groupId=${TEAM.id}&memberId=${member.id}&companyId=${site.companyId}`),
      json(`/api/solutions?groupId=${TEAM.id}&memberId=${member.id}&companyId=${site.companyId}`),
      json(`/api/reference-questions?companyId=${site.companyId}`),
      json(`/api/research-dashboard?memberId=${member.id}`)
    ]);
    setQuestions(questionData.questions ?? []); setQuestionDraft(null); setProblems(problemData.problems ?? []); setSolutions(solutionData.solutions ?? []); setReference(referenceData); setDashboard(dashboardData);
  }
  useEffect(() => { void loadSite(); }, [site?.companyId]);

  async function suggestQuestions() {
    if (!site) return; setBusy("suggest"); setStatus("");
    try { const result = await json("/api/research-report/suggest", jsonOptions("POST", { memberId:member.id, companyId:site.companyId })); setQuestions(result.questions ?? []); setQuestionDraft({dimensions:result.dimensions??[],mode:result.mode,previousCount:questions.length}); setStatus(`已生成 ${result.questions?.length ?? 0} 个新问题，覆盖 ${result.dimensionCount ?? 0} 个调研维度。当前仅为草稿，点击保存后才会替换已保存清单。`); }
    catch (error) { setStatus(error.message); } finally { setBusy(""); }
  }
  async function saveQuestions() {
    if (!site) return; setBusy("questions");
    try { const result=await json("/api/research-questions",jsonOptions("PUT",{memberId:member.id,companyId:site.companyId,questions}));setQuestions(result.questions);setQuestionDraft(null);setStatus("问题清单已保存并同步到报告工作台。"); }
    catch(error){setStatus(error.message)}finally{setBusy("")}
  }
  function addQuestion(){setQuestions(current=>[...current,{id:`q-local-${Date.now()}`,text:"",tags:["待验证"],lens:"pending"}])}
  function updateQuestion(index, patch){setQuestions(current=>current.map((item,i)=>i===index?{...item,...patch}:item))}
  function addReferenceQuestions(items) {
    const existing = new Set(questions.map(item => item.text.trim()));
    const additions = items.filter(item => !existing.has(item.text.trim())).map(item => ({ ...item, id:`q-reference-${item.id}-${Date.now()}`, validationOutcome:"pending", evidenceSource:"", answer:"", lens:"pending" }));
    setQuestions(current => [...current, ...additions]);
    setQuestionDraft({ dimensions:["外部路线参考"], mode:"reference", previousCount:questions.length });
    setStatus(additions.length ? `已加入 ${additions.length} 条参考问题草稿。保存后仍是待验证状态，不计入验证比例。` : "这些参考问题已在当前清单中。");
  }

  async function upload(event) {
    event.preventDefault(); if (!site || (!text.trim() && !fileRef.current?.files?.[0])) return;
    const form = new FormData(); form.set("memberId",member.id);form.set("memberName",member.name);form.set("groupId",TEAM.id);form.set("companyId",site.companyId);form.set("companyName",site.companyName);
    const file=fileRef.current?.files?.[0];form.set("type",file?.type?.startsWith("audio/")?"audio":file?"image":"text");form.set("evidenceKind",evidenceKind);form.set("title","现场留痕");form.set("caption",text.trim());if(text.trim())form.set("textContent",text.trim());if(file)form.set("file",file);
    setBusy("upload");setStatus("上传中…");const response=await fetch("/api/media/upload",{method:"POST",body:form});setStatus(response.ok?"已同步到留痕库与证据链":"上传失败");if(response.ok){setText("");if(fileRef.current)fileRef.current.value="";await loadSite()}setBusy("");
  }
  async function addProblem(event){event.preventDefault();if(!site)return;await json("/api/problems",jsonOptions("POST",{...problem,groupId:TEAM.id,memberId:member.id,memberName:member.name,companyId:site.companyId,companyName:site.companyName,validationOutcome:"pending"}));setProblem({title:"",evidence:"",severity:"normal"});setStatus("痛点已加入田野问题库。");await loadSite()}
  async function addSolution(event){event.preventDefault();if(!site)return;const confirmed=problems.filter(item=>item.validationOutcome==="confirmed");if(!confirmed.length){setStatus("请先确认至少一条痛点，再创建试点方案。");return}await json("/api/solutions",jsonOptions("POST",{...solution,metrics:solution.metrics.split(/[，,]/).map(value=>value.trim()).filter(Boolean),groupId:TEAM.id,memberId:member.id,memberName:member.name,companyId:site.companyId,companyName:site.companyName,linkedProblemIds:confirmed.slice(0,5).map(item=>item.id),validationStatus:"draft"}));setSolution({title:"",description:"",testPlan:"",metrics:""});setStatus("方案已创建并挂钩已确认痛点，下一步开始试验。");await loadSite()}
  async function synthesize(action){if(!site)return;setBusy(`synth-${action}`);setStatus("");try{const result=await json("/api/evidence/synthesize",jsonOptions("POST",{action,memberId:member.id,memberName:member.name,companyId:site.companyId,companyName:site.companyName,observations}));if(action==="observations"){setObservations(result.observations??[]);setStatus(`已从留痕提取 ${result.observations?.length??0} 条观察点，请人工复核。`)}else if(action==="problem")setStatus("已将观察点收敛为待确认痛点。");else if(action==="solution")setStatus("已基于确认痛点生成关联方案。");else setStatus(`已将证据链加入报告，共引用 ${result.addedRefs??0} 条记录。`);await loadSite()}catch(error){setStatus(error.message)}finally{setBusy("")}}
  async function validateProblem(id,outcome){try{await json(`/api/problems/${id}`,jsonOptions("PATCH",{validationOutcome:outcome,validationNote:problemNotes[id]}));setStatus(outcome==="confirmed"?"痛点已由队员确认，可用于生成方案。":"痛点已标记为不成立，证伪过程仍会保留。");await loadSite()}catch(error){setStatus(error.message)}}
  async function validateSolution(id,validationStatus){try{await json(`/api/solutions/${id}`,jsonOptions("PATCH",{validationStatus,validationResult:solutionResults[id]}));setStatus(validationStatus==="testing"?"方案已进入试验，记得记录基线与过程指标。":validationStatus==="validated"?"方案已验证有效，可加入报告。":"方案已进入迭代，当前歧路与结果已保留。");await loadSite()}catch(error){setStatus(error.message)}}

  if (!dashboard) return <Loading label="加载参访路线…"/>;
  const validatedQuestions=questions.filter(item=>item.validationOutcome&&item.validationOutcome!=="pending");
  const confirmedProblems=problems.filter(item=>item.validationOutcome==="confirmed");
  const testedSolutions=solutions.filter(item=>["validated","iterate"].includes(item.validationStatus));
  const questionPercent=questions.length?Math.round(validatedQuestions.length/questions.length*100):0;
  const liveClosure=Math.round((questions.length?15:0)+(questions.length?validatedQuestions.length/questions.length*25:0)+(site.evidenceCount?20:0)+(confirmedProblems.length?15:0)+(solutions.some(item=>item.linkedProblemIds?.length)?15:0)+(testedSolutions.length?10:0));
  return <div className="route-capture"><header><div><MapPin size={23}/><span><small>{member.name} · 三日调研路线</small><h1>现场调研工作台</h1></span></div><StatusDot label="本地服务已连接"/></header><div className="site-scroller">{sites.map(item=><button className={item.companyId===active?"active":""} onClick={()=>{setActive(item.companyId);setPanel("questions")}} key={`${item.routeId}:${item.companyId}`}><strong>{item.companyName}</strong><small>Day {item.day} · {item.routeLabel} · {item.themeName}</small></button>)}</div>{site?<section className="field-workspace"><header><div><small>当前站点 · Day {site.day} · {site.routeLabel}</small><h2>{site.companyName}</h2><p>{site.activity} · {site.themeName}</p></div><div className="site-closure-score"><span>闭环完成度</span><strong>{liveClosure}%</strong><AppLink href={`/dashboard?memberId=${member.id}&companyId=${site.companyId}`}>打开报告</AppLink></div></header><div className="closure-progress">{[["01","预设问题",questions.length],["02","逐题验证",`${questionPercent}%`],["03","确认痛点",confirmedProblems.length],["04","试点方案",solutions.length],["05","验证迭代",testedSolutions.length]].map(([number,label,count],index)=><React.Fragment key={label}><button className={(index===1?validatedQuestions.length:count)?"complete":""} onClick={()=>setPanel(index<2?"questions":"evidence")}><small>{number}</small><span>{label}<strong>{count}</strong></span></button>{index<4?<i>→</i>:null}</React.Fragment>)}</div><Tabs value={panel} onChange={setPanel} items={[{value:"questions",label:`预设与验证 ${validatedQuestions.length}/${questions.length}`},{value:"capture",label:`现场留痕 ${site.evidenceCount||0}`},{value:"evidence",label:`痛点与方案 ${problems.length}/${solutions.length}`}]}/>
    {panel==="questions"?<div className="field-questions"><div className="field-panel-actions"><button onClick={addQuestion}><Plus size={15}/>添加问题</button><button onClick={()=>void suggestQuestions()} disabled={!!busy}><Lightbulb size={15}/>{busy==="suggest"?"生成中…":"AI 生成 18 问"}</button><button className="primary-button" onClick={()=>void saveQuestions()} disabled={!!busy}><CheckCircle2 size={15}/>保存问题与验证</button></div>{reference.matched?<section className="route-reference"><header><div><ExternalLink size={16}/><span><strong>重合路线参考问题</strong><small>Drop 第九组 · 外部线索 · 不计入第八组现场证据</small></span></div><button onClick={()=>addReferenceQuestions(reference.questions)}>全部加入草稿</button></header><div className="reference-question-list">{reference.questions.map(item=><article key={item.id}><span><ShieldCheck size={14}/>待现场复核</span><p>{item.text}<small>{item.dimension} · {item.target} · {item.method}</small></p><button onClick={()=>addReferenceQuestions([item])}><Plus size={14}/>加入</button></article>)}</div>{reference.clues.length?<details><summary>查看 {reference.clues.length} 条外部观察线索</summary>{reference.clues.map(item=><p key={item.id}>{item.text}</p>)}<small>这些线索不能直接粘贴为本组证据，请在现场找到照片、原声或记录 ID 后再验证。</small></details>:null}</section>:null}{questionDraft?<div className="question-draft-notice"><Bot size={16}/><span><strong>{questionDraft.mode==="reference"?"参考问题草稿":"AI 新草稿"} · 尚未保存</strong><small>覆盖 {questionDraft.dimensions.length} 个维度{questionDraft.previousCount?` · 原已保存 ${questionDraft.previousCount} 问仍在服务器中`:""}</small></span><div>{questionDraft.dimensions.slice(0,6).map(item=><i key={item}>{item}</i>)}</div></div>:null}{questions.map((question,index)=><article className={`${question.validationOutcome&&question.validationOutcome!=="pending"?`validated ${question.validationOutcome}`:""} ${question.tags?.includes("external-reference")?"external-reference":""}`} key={question.id||index}><span>{index+1}</span><div className="question-editor"><textarea value={question.text||""} onChange={event=>updateQuestion(index,{text:event.target.value})} placeholder="输入一个可在现场观察或访谈验证的问题"/><small>{question.tags?.includes("external-reference")?"外部参考 · 待本组现场复核 · ":""}{[question.dimension,question.target,question.method].filter(Boolean).join(" · ")}</small><div className="question-validation"><select aria-label={`第 ${index+1} 题验证结论`} value={question.validationOutcome||"pending"} onChange={event=>updateQuestion(index,{validationOutcome:event.target.value})}><option value="pending">尚未验证</option><option value="confirmed">现场成立</option><option value="partial">部分成立</option><option value="refuted">现场证伪</option></select><input value={question.evidenceSource||""} onChange={event=>updateQuestion(index,{evidenceSource:event.target.value})} placeholder="证据来源：照片、原声或记录 ID"/><input value={question.answer||""} onChange={event=>updateQuestion(index,{answer:event.target.value})} placeholder="写下验证结论或认知变化"/></div></div><select aria-label={`第 ${index+1} 题研究视角`} value={question.lens||"pending"} onChange={event=>updateQuestion(index,{lens:event.target.value})}><option value="pending">未分组</option><option value="pioneer">开拓</option><option value="iterate">迭代</option></select><button title="删除" onClick={()=>setQuestions(current=>current.filter((_,i)=>i!==index))}><Trash2 size={15}/></button></article>)}{!questions.length?<Empty/>:null}</div>:null}
    {panel==="capture"?<div className="capture-stack"><form className="capture-card" onSubmit={upload}><select value={evidenceKind} onChange={event=>setEvidenceKind(event.target.value)}><option value="observation">现场观察</option><option value="quote">访谈原声</option><option value="metric">数字指标</option><option value="anomaly">异常流程</option><option value="pending">待验证判断</option></select><textarea value={text} onChange={event=>setText(event.target.value)} placeholder="记录现场观察、访谈原声、数字、异常流程或待验证结论…"/><label className="file-button"><Camera size={19}/><Mic size={19}/><span>选择图片或录音</span><input ref={fileRef} type="file" accept="image/*,audio/*"/></label><button className="primary-button" disabled={busy==="upload"}><Upload size={17}/>{busy==="upload"?"上传中…":"上传并进入证据链"}</button></form><section className="synthesis-panel"><header><Bot size={17}/><div><h3>Agent 证据推进</h3><p>逐题验证和现场留痕共同进入证据链，证伪结果同样保留。</p></div></header><div><button disabled={!!busy} onClick={()=>void synthesize("observations")}>1. 从留痕生成观察点</button><button disabled={!!busy||(!observations.length&&!validatedQuestions.length)} onClick={()=>void synthesize("problem")}>2. 从验证记录收敛痛点</button><button disabled={!!busy||!confirmedProblems.length} onClick={()=>void synthesize("solution")}>3. 从确认痛点生成试点</button><button disabled={!!busy} onClick={()=>void synthesize("report")}>4. 一键加入四核报告</button></div><p className="synthesis-summary">已验证问题 {validatedQuestions.length} · 提炼观察 {observations.length} · 确认痛点 {confirmedProblems.length}</p>{observations.length?<ol>{observations.map((item,index)=><li key={`${item.sourceId}-${index}`}><span>{item.kind}</span>{item.text}<small>{item.sourceId}</small></li>)}</ol>:null}</section></div>:null}
    {panel==="evidence"?<div className="closure-workspace"><div className="field-evidence"><form onSubmit={addProblem}><h3><Lightbulb size={16}/>补充田野痛点</h3><input required value={problem.title} onChange={event=>setProblem({...problem,title:event.target.value})} placeholder="痛点标题"/><textarea required value={problem.evidence} onChange={event=>setProblem({...problem,evidence:event.target.value})} placeholder="支持该痛点的现场事实、原声或问题验证记录"/><select value={problem.severity} onChange={event=>setProblem({...problem,severity:event.target.value})}><option value="normal">一般</option><option value="high">高优先级</option></select><button className="primary-button">加入问题库</button></form><form onSubmit={addSolution}><h3><Link2 size={16}/>设计最小试点</h3><input required value={solution.title} onChange={event=>setSolution({...solution,title:event.target.value})} placeholder="方案标题"/><textarea required value={solution.description} onChange={event=>setSolution({...solution,description:event.target.value})} placeholder="方案如何回应已确认痛点"/><textarea required value={solution.testPlan} onChange={event=>setSolution({...solution,testPlan:event.target.value})} placeholder="试验方法、基线、周期与人工兜底"/><input required value={solution.metrics} onChange={event=>setSolution({...solution,metrics:event.target.value})} placeholder="验证指标，用逗号分隔"/><small>提交时只挂钩当前站点已确认的 {confirmedProblems.length} 条痛点</small><button className="primary-button" disabled={!confirmedProblems.length}>创建并挂钩方案</button></form></div><section className="closure-ledger"><header><div><h3>问题 → 方案验证账本</h3><p>成立、证伪、试验失败与迭代路径全部留痕</p></div><strong>{testedSolutions.length}/{solutions.length} 已得出试验结论</strong></header><div className="ledger-columns"><div><h4>田野问题库</h4>{problems.length?problems.slice(0,8).map(item=><article className={`ledger-problem ${item.validationOutcome||"pending"}`} key={item.id}><header><strong>{item.title}</strong><span>{item.validationOutcome==="confirmed"?"已确认":item.validationOutcome==="rejected"?"已证伪":"待确认"}</span></header><p>{item.evidence}</p><small>关联逐题验证 {item.questionIds?.length||0} · 观察证据 {item.observationIds?.length||0}</small>{item.validationOutcome==="pending"?<div><input value={problemNotes[item.id]||""} onChange={event=>setProblemNotes(current=>({...current,[item.id]:event.target.value}))} placeholder="填写人工验证依据"/><button onClick={()=>void validateProblem(item.id,"confirmed")}>确认成立</button><button onClick={()=>void validateProblem(item.id,"rejected")}>现场证伪</button></div>:<em>{item.validationNote||"已保留验证结论"}</em>}</article>):<Empty/>}</div><div><h4>关联方案与试验</h4>{solutions.length?solutions.slice(0,8).map(item=><article className={`ledger-solution ${item.validationStatus||"draft"}`} key={item.id}><header><strong>{item.title}</strong><span>{item.validationStatus==="testing"?"试验中":item.validationStatus==="validated"?"验证有效":item.validationStatus==="iterate"?"需迭代":"待试验"}</span></header><p>{item.description}</p><small>挂钩痛点 {item.linkedProblemIds?.length||0} · 指标 {(item.metrics||[]).join(" / ")||"待补充"}</small><p className="test-plan">{item.testPlan||"尚未填写试验计划"}</p>{["validated","iterate"].includes(item.validationStatus)?<em>{item.validationResult}</em>:<div><input value={solutionResults[item.id]||""} onChange={event=>setSolutionResults(current=>({...current,[item.id]:event.target.value}))} placeholder="记录基线、试验结果或认知变化"/>{item.validationStatus!=="testing"?<button onClick={()=>void validateSolution(item.id,"testing")}>开始试验</button>:null}<button disabled={!solutionResults[item.id]} onClick={()=>void validateSolution(item.id,"validated")}>验证有效</button><button disabled={!solutionResults[item.id]} onClick={()=>void validateSolution(item.id,"iterate")}>保留并迭代</button></div>}</article>):<Empty/>}</div></div></section></div>:null}{status?<p className="form-status field-status">{status}</p>:null}</section>:<Empty/>}</div>;
}
