# 内容补齐发现

## 生产快照

- 备份：164 `backups/20260717_182440/`。
- 金俊烯：10 站，80 问，51 已验证，64%，70 条留痕，1 份报告。
- 林耿标：8 站，104 问，84 已验证，81%，56 条留痕，2 份报告。
- 钟宝怡：10 站，70 问，58 已验证，83%，40 条留痕，0 份报告。
- 韩昆桦：10 站，41 问，41 已验证，100%，11 条留痕，0 份报告。
- 苏增烨：10 站，158 问，158 已验证，100%，20 条留痕，10 份报告。

## 实现约束

- 生产问题保存在 `researchQuestions[memberId::companyId]`。
- 验证状态仅接受 `confirmed`、`partial`、`refuted`；非 pending 时必须有 `evidenceSource` 或 `answer`。
- 报告保存在 `generatedReports[memberId::companyId]`，四核结构为 situation、empathy、painPoints、conception。
- `插入内容.md` 已有解析脚本，但原脚本仅导入 pending 问题，不能满足本轮验证和报告补齐需求。
- 原文存在重复问题和把公开资料写成现场事实的段落，本轮不能原样照搬；只能作为历史参考并标注证据等级。

## 逐站缺口

- 金俊烯：29 个 pending，主要集中在成至、得实、夏朋；9 站缺报告。
- 林耿标：20 个 pending，主要集中在信源、安必平、盘福、中大；6 站缺报告。
- 钟宝怡：12 个 pending；10 站全部缺报告。
- 韩昆桦：已有问题均验证，但广柴、中铁、中建、八位堂、南湾慧库、粤湾云谷等站点问题或证据不足，10 站全部缺报告。
- 苏增烨：问题和报告均已完成，本轮只作为同站点组内共享材料来源。

## 知识库污染

- 唯一明确旧组条目为 `kb-team-charter`，标题“流光九径 · 第九组调研宪章”。
- 旧条目包含段辉、陈佳卉、罗子航、王馨妍、叶泽瑞，必须删除或原 ID 原位替换。
- 其余知识文档均为第八组官方路线节点和当前成员配置，可保留。

## 写入后验收

- 五人均为 100% 逐题验证，合计 501/501。
- 48/48 成员站点都有问题，48/48 都有报告。
- 旧组关键词命中为 0；`kb-team-charter` 已替换为“红八宝 · 八爪鱼组调研宪章”。
- 再次 dry-run 的问题、证据、报告、宪章改动均为 0，确认幂等。
- 抽查 4 份报告均有 16/16 小节，但接口完整度仍显示 19%/31%，原因是保存的基础报告保留旧 completeness；已进入代码修复。
- 完整度修复部署后，4 份抽查报告均为 16/16、100%、ready=true。
- 红八宝 Agent 实测使用 DeepSeek 正确回答第八组成员与证据规则，引用 `kb-team-charter` 新宪章，不再引用旧组。
- 最终生产备份：164 `backups/20260717_183830/`；501/501 问、48 报告、205 留痕、24 知识、旧知识 0。

## 管理员定稿文件名乱码

- 根因是 Busboy 默认 `defParamCharset=latin1`，浏览器发送的 UTF-8 中文 `filename` 被按 Latin-1 解码后直接写入 JSON。
- 当前 8 份苏增烨“管理员定稿 v3”的 `originalName` 和版本 `filename` 均为同类乱码；`storedName`、文件体积和 DOCX 内容未损坏。
- 修复范围必须覆盖上传保存、管理端展示和 DOCX 下载文件名。
- 现有管理员定稿只修正元数据，不重新上传或覆盖文件正文。
- 上传端现已显式使用 UTF-8 解析 multipart 参数，并对历史 Latin-1 乱码、控制字符和路径字符做规范化。
- 8 份历史定稿的 `originalName` 与版本 `filename` 已原子修正，所有 `storedName` 文件仍存在，DOCX 正文未变。
- 管理端现在显示“管理员定稿 v3”，撤下接口统一使用 `memberId::companyId`，并对不存在的记录返回 404。
- `reportReadyCount` 按成员与站点唯一键去重，AI 报告和管理员定稿不会再重复计数。
- 生产持久化状态已正确显示为服务器本地 JSON 与 uploads，不再误报未连接生产存储。
- 发布后生产基线保持 501/501 问、48/48 报告、205 留痕、24 知识；备份为 164 `backups/20260717_185625/`。
- 浏览器验收通过：桌面评审、知识中心、红八宝与手机 390x844 评审页无横向溢出、破图或控制台应用错误。
